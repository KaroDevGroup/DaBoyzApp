<Page x:Class="DaBoyzApp.Pages.BuildsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
      Background="Transparent">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <!-- HEADER -->

        <Grid Grid.Row="0"
              Margin="10,5,10,15">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>


            <!-- TITLE -->

            <StackPanel Grid.Column="0">

                <TextBlock Text="DESTINY 2  /  BUILDS"
                           FontSize="13"
                           FontWeight="SemiBold"
                           Foreground="#B0B0B0"/>

                <TextBlock Text="BUILDS"
                           FontSize="30"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,2,0,0"/>

                <TextBlock Text="Explore powerful Destiny 2 builds for every class and activity."
                           FontSize="13"
                           Foreground="#B0B0B0"
                           Margin="0,5,0,0"/>

            </StackPanel>


            <!-- BACK BUTTON -->

            <Button Grid.Column="1"
                    Content="BACK TO DESTINY 2"
                    Width="180"
                    Height="40"
                    VerticalAlignment="Center"
                    Background="#161616"
                    Foreground="#B0B0B0"
                    BorderBrush="#292929"
                    BorderThickness="1"
                    FontSize="11"
                    FontWeight="SemiBold"
                    Padding="18,10"
                    Cursor="Hand"
                    Click="BackButton_Click"/>

        </Grid>


        <!-- WEBVIEW2 -->

        <Border Grid.Row="1"
                Background="#101010"
                BorderBrush="#292929"
                BorderThickness="1">

            <wv2:WebView2 x:Name="BuildsBrowser"/>

        </Border>

    </Grid>

</Page>