<!-- KaroDevGroup -->
<!-- Josh Karo -->

<Page x:Class="DaBoyzApp.Pages.ClearMessagesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="Transparent">

        <Grid>

            <!-- ================================================= -->
            <!-- HEADER                                            -->
            <!-- ================================================= -->

            <StackPanel Margin="45,40,45,50">

                <Grid>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>

                        <TextBlock Text="CLEAR MESSAGES"
                                   Foreground="{StaticResource DaBoyzText}"
                                   FontSize="32"
                                   FontWeight="Bold"/>

                        <TextBlock Text="Delete messages from a Discord channel through DaBoyzApp."
                                   Foreground="{StaticResource DaBoyzTextSecondary}"
                                   FontSize="14"
                                   Margin="0,5,0,0"/>

                    </StackPanel>

                    <Button Grid.Column="1"
                            Content="BACK TO DECISIONS"
                            Width="180"
                            Height="40"
                            Margin="30,0,0,0"
                            VerticalAlignment="Center"
                            Background="#161616"
                            Foreground="{StaticResource DaBoyzTextSecondary}"
                            BorderBrush="{StaticResource DaBoyzBorder}"
                            BorderThickness="1"
                            FontSize="11"
                            Padding="18,10"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="BackButton_Click"/>

                </Grid>

                <Border Height="2"
                        Width="80"
                        Background="{StaticResource DaBoyzRed}"
                        HorizontalAlignment="Left"
                        Margin="0,18,0,0"/>

            </StackPanel>


            <!-- ================================================= -->
            <!-- CENTER CLEAR CARD                                 -->
            <!-- ================================================= -->

            <Border Width="520"
                    BorderBrush="#333333"
                    BorderThickness="1"
                    CornerRadius="16"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    ClipToBounds="True">

                <Grid>

                    <!-- DARK BASE -->

                    <Border Background="#161616"
                            CornerRadius="16"/>

                    <!-- GRUNGE FORM BACKGROUND -->

                    <Border Background="{StaticResource FormCardBackgroundImage}"
                            CornerRadius="16"
                            Opacity="0.40"/>

                    <!-- READABILITY OVERLAY -->

                    <Border Background="#50000000"
                            CornerRadius="16"/>

                    <!-- CONTENT -->

                    <StackPanel Margin="40">

                        <StackPanel HorizontalAlignment="Center"
                                    Margin="0,0,0,30">

                            <TextBlock Text="DA BOYZ"
                                       Foreground="White"
                                       FontSize="28"
                                       FontWeight="Bold"
                                       HorizontalAlignment="Center"/>

                            <TextBlock Text="MODERATION"
                                       Foreground="{StaticResource DaBoyzRed}"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Margin="0,4,0,0"/>

                        </StackPanel>


                        <TextBlock Text="Clear channel messages."
                                   Foreground="{StaticResource DaBoyzText}"
                                   FontSize="24"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"/>

                        <TextBlock Text="Enter the channel ID and number of messages to delete."
                                   Foreground="{StaticResource DaBoyzTextSecondary}"
                                   FontSize="13"
                                   HorizontalAlignment="Center"
                                   TextAlignment="Center"
                                   Margin="0,6,0,30"/>


                        <!-- CHANNEL ID -->

                        <TextBlock Text="DISCORD CHANNEL ID"
                                   Foreground="{StaticResource DaBoyzTextSecondary}"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,7"/>

                        <Border Background="#111111"
                                BorderBrush="#292929"
                                BorderThickness="1"
                                CornerRadius="8"
                                Height="45"
                                Margin="0,0,0,18">

                            <TextBox x:Name="ChannelIdBox"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="White"
                                     FontSize="13"
                                     Padding="14,0"
                                     VerticalContentAlignment="Center"/>

                        </Border>


                        <!-- AMOUNT -->

                        <TextBlock Text="MESSAGE AMOUNT"
                                   Foreground="{StaticResource DaBoyzTextSecondary}"
                                   FontSize="11"
                                   FontWeight="SemiBold"
                                   Margin="0,0,0,7"/>

                        <Border Background="#111111"
                                BorderBrush="#292929"
                                BorderThickness="1"
                                CornerRadius="8"
                                Height="45"
                                Margin="0,0,0,5">

                            <TextBox x:Name="AmountBox"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     Foreground="White"
                                     FontSize="13"
                                     Padding="14,0"
                                     VerticalContentAlignment="Center"/>

                        </Border>

                        <TextBlock Text="Minimum: 1 • Maximum: 100"
                                   Foreground="{StaticResource DaBoyzTextMuted}"
                                   FontSize="10"
                                   Margin="2,0,0,22"/>


                        <!-- SUBMIT -->

                        <Button x:Name="ClearButton"
                                Content="CLEAR MESSAGES"
                                Height="45"
                                Background="{StaticResource DaBoyzRed}"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="13"
                                FontWeight="Bold"
                                Cursor="Hand"
                                Click="ClearButton_Click"/>


                        <!-- STATUS -->

                        <TextBlock x:Name="StatusText"
                                   Foreground="{StaticResource DaBoyzRed}"
                                   FontSize="12"
                                   TextAlignment="Center"
                                   HorizontalAlignment="Center"
                                   TextWrapping="Wrap"
                                   Margin="0,15,0,0"/>


                        <!-- FOOTER -->

                        <TextBlock Text="AUTHORIZED STAFF ONLY"
                                   Foreground="{StaticResource DaBoyzTextMuted}"
                                   FontSize="9"
                                   FontWeight="SemiBold"
                                   HorizontalAlignment="Center"
                                   Margin="0,25,0,0"/>

                    </StackPanel>

                </Grid>

            </Border>

        </Grid>

    </Grid>

</Page>